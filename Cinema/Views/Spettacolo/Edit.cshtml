@model Cinema.Models.Spettacolo

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Spettacolo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Data" />
            <input type="hidden" asp-for="Ora" />
            <div class="form-group">
                <label asp-for="IdFilm" class="control-label"></label>
                <select asp-for="IdFilm" class="form-control" asp-items="ViewBag.IdFilm"></select>
                <span asp-validation-for="IdFilm" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="IdSala" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
